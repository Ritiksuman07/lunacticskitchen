# MVP Screenshots

This project includes two key visual checkpoints for release review:

1. **Home/Menu state** (filters + menu grid)
2. **Checkout/Payment state** (final price transparency)

Screenshots are generated during release validation via Playwright and attached as CI/agent artifacts in PR delivery notes.
